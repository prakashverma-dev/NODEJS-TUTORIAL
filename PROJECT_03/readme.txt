

#PROJECT_03 : Stripe Payment Gateway Integration with Frontend : -

start the project with >npm init -y

Then, We need modules/dependencies like -

>npm i express ejs dotenv nodemon stripe

Make .env file and .gitignore(mention two file name i.e node_modules and
.env )